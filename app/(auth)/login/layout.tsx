export const metadata = {
    title: 'Login | Coin Share',
    description: 'Welcome Back, login to enter coin share',
  }
  
  export default function RootLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (children)
  }