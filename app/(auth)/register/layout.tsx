export const metadata = {
    title: 'Register | Coin Share',
    description: 'Create Account, enter details below to create your coin share account',
}

export default function RootLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (children)
}