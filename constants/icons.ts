import {
  IndianRupee,
  ReceiptText,
  Activity,
  ArrowUpRight,
} from 'lucide-react';

export const icons = {
  IndianRupee,
  ReceiptText,
  Activity,
  ArrowUpRight,
};

export type IconKey = keyof typeof icons;